<?xml version="1.0" encoding="utf-8"?>
<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Base Theme -->
    <style name="Theme.ToDoAppFinal.Base" parent="Theme.MaterialComponents.Light.NoActionBar">
        <item name="colorPrimary">@color/purple_500</item>
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorOnPrimary">@color/white</item>
        <item name="android:colorBackground">@color/light_blue</item>
        <item name="android:statusBarColor">@color/purple_700</item>
        <item name="android:windowLightStatusBar">true</item>
    </style>

    <!-- Main App Theme inheriting from Base -->
    <style name="Theme.ToDoAppFinal" parent="Theme.ToDoAppFinal.Base"/>

    <!-- NoActionBar Theme inheriting from Base -->
    <style name="Theme.ToDoAppFinal.NoActionBar" parent="Theme.ToDoAppFinal.Base"/>

</resources>
